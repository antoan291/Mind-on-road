{
  "name": "mindonroad-backend",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "5.6.3"
  },
  "scripts": {
    "start": "node dist/main.js",
    "start:dev": "node --watch dist/main.js",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "prisma:generate": "prisma generate",
    "prisma:migrate:dev": "prisma migrate dev"
  }
}
